<Controls:MetroWindow x:Class="YoutubePlaylistDownloader.Skeleton"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
                      MinWidth="1280"
                      MinHeight="720"
                      Width="1280"
                      Height="720"
                      x:Name="SkeletonWindow"
                      GlowBrush="White"
                      BorderThickness="0"
                      Title="{DynamicResource Title}"
                      FlowDirection="{DynamicResource FlowDirection}">

    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout IsPinned="False" HorizontalAlignment="Left" VerticalAlignment="Top" Header="" x:Name="DefaultFlyout" Position="Left" Width="Auto">
                <Controls:TransitioningContentControl HorizontalAlignment="Left" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" x:Name="DefaultFlyoutUserControl" />
            </Controls:Flyout>
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>




    <!--Set all the UI in 1 UserControl with this window being the skeleton-->
    <Grid VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderThickness="0,0,1,0" BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}" Background="{DynamicResource MahApps.Brushes.Control.Background}">
            <Grid Margin="10,24,10,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <iconPacks:PackIconModern Width="28" Height="28" Kind="Youtube" Foreground="{DynamicResource MahApps.Brushes.Accent}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="YPD Downloader" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" VerticalAlignment="Center" />
                    </StackPanel>
                    <Rectangle Height="1" Fill="{DynamicResource MahApps.Brushes.Control.Border}" Margin="10,0" Opacity="0.5"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Top">
                    <Controls:Tile Click="Home_Click" x:Name="HomeButton" Style="{StaticResource SidebarTileStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="20" Height="20" Kind="Home" VerticalAlignment="Center" Margin="15,0,15,0" />
                            <TextBlock Text="{DynamicResource Home}" VerticalAlignment="Center" FontSize="15" />
                        </StackPanel>
                    </Controls:Tile>

                    <Controls:Tile Click="Settings_Click" x:Name="SettingsButton" Style="{StaticResource SidebarTileStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="20" Height="20" Kind="Settings" VerticalAlignment="Center" Margin="15,0,15,0" />
                            <TextBlock Text="{DynamicResource Settings}" VerticalAlignment="Center" FontSize="15" />
                        </StackPanel>
                    </Controls:Tile>

                    <Controls:Tile Click="Help_Click" x:Name="HelpButton" Style="{StaticResource SidebarTileStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="20" Height="20" Kind="BookPerspectiveHelp" VerticalAlignment="Center" Margin="15,0,15,0" />
                            <TextBlock Text="{DynamicResource Help}" VerticalAlignment="Center" FontSize="15" />
                        </StackPanel>
                    </Controls:Tile>

                    <Controls:Tile Click="About_Click" x:Name="AboutButton" Style="{StaticResource SidebarTileStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="20" Height="20" Kind="AppPlus" VerticalAlignment="Center" Margin="15,0,15,0" />
                            <TextBlock Text="{DynamicResource About}" VerticalAlignment="Center" FontSize="15" />
                        </StackPanel>
                    </Controls:Tile>

                    <Controls:Tile Click="MiniPlayer_Click" x:Name="MiniPlayerButton" Style="{StaticResource SidebarTileStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="20" Height="20" Kind="MonitorPlay" VerticalAlignment="Center" Margin="15,0,15,0" />
                            <TextBlock Text="{DynamicResource MiniPlayer}" VerticalAlignment="Center" FontSize="15" />
                        </StackPanel>
                    </Controls:Tile>
                </StackPanel>

                <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                    <TextBlock Text="v1.9.33" FontSize="11" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Opacity="0.5" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
            <Controls:TransitioningContentControl Height="Auto" HorizontalAlignment="Stretch" x:Name="CurrentPage" Margin="0" Transition="Default" VerticalAlignment="Stretch" />
        </Grid>

        <Grid x:Name="ModalOverlay" Grid.ColumnSpan="2" Visibility="Collapsed" Panel.ZIndex="1000">
            <Border Background="Black" Opacity="0.5"/>
            
            <Border Background="{DynamicResource MahApps.Brushes.ThemeBackground}" 
                    BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}" 
                    BorderThickness="1" 
                    CornerRadius="10" 
                    Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center" 
                    MinWidth="450" MaxWidth="550" Padding="30">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.3" />
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                        <iconPacks:PackIconModern x:Name="ModalIcon" Kind="Warning" Width="36" Height="36" Foreground="{DynamicResource MahApps.Brushes.Accent}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="ModalTitle" FontSize="26" FontWeight="SemiBold" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock x:Name="ModalMessage" Grid.Row="1" FontSize="16" TextWrapping="Wrap" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Margin="0,0,0,30" Opacity="0.9" LineHeight="24"/>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="ModalYesButton" Click="ModalYes_Click" Content="{DynamicResource Yes}" Margin="0,0,15,0" MinWidth="120" Height="40" Background="{DynamicResource MahApps.Brushes.Accent}" Foreground="{DynamicResource MahApps.Brushes.ThemeBackground}" BorderThickness="0" FontSize="16" FontWeight="SemiBold" Cursor="Hand" />
                        <Button x:Name="ModalAuxButton" Click="ModalAux_Click" Visibility="Collapsed" Margin="0,0,15,0" MinWidth="120" Height="40" FontSize="16" Cursor="Hand" />
                        <Button x:Name="ModalNoButton" Click="ModalNo_Click" Content="{DynamicResource No}" MinWidth="100" Height="40" FontSize="16" Cursor="Hand" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>

</Controls:MetroWindow>
